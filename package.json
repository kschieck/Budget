{
    "scripts": {
        "build": "gulp",
        "build-no-zip": "gulp build-no-zip",
        "prettier": "prettier --config ./.prettierrc --write --list-different \"client*/**/*.js\""
    },
    "devDependencies": {
        "gulp": "^5.0.0",
        "gulp-clean": "^0.4.0",
        "gulp-zip": "^5.1.0",
        "gulp-shell": "0.8.0"
    }
}